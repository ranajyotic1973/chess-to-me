node_modules/
dist/
electron/dist/
release/
*.log
*.tmp
*.cache
*.exe
*.dll
*.pdb
data/puzzles/*.db
data/puzzles/.version
data/games/*.db
data/games/.version

# Test reports and artifacts (keep the repo compact — never upload these)
playwright-report/
test-results/
coverage/

# Graphify regenerable cache (AST/semantic) — rebuilt locally, not needed in git.
# The queryable graph (graph.json, GRAPH_REPORT.md, etc.) stays tracked.
graphify-out/cache/
